# Consumer acknowledgement timeout
# If a message delivered to a consumer has not been acknowledged before this timer
# triggers, the channel will be force closed by the broker.
#
# Default: 30 minutes (1800000 ms)
# Changed to: 12 hours (43200000 ms) to allow long-running inference jobs
consumer_timeout = 43200000
